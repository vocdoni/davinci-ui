import { Averia_Libre, Work_Sans } from 'next/font/google'

export const averiaLibre = Averia_Libre({
  subsets: ['latin'],
  weight: ['400', '700'],
  variable: '--font-averia-libre',
})

export const workSans = Work_Sans({
  subsets: ['latin'],
  variable: '--font-work-sans',
})
